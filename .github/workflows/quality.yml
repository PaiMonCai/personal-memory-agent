name: Quality checks

on:
  push:
    branches: [main, "fix/**", "refactor/**"]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: npm install --package-lock=false

      - name: JavaScript syntax (server)
        run: find server/src -name '*.js' -print0 | xargs -0 -n1 node --check

      - name: TypeScript check
        run: npm run check

      # PR 上验证两个 Dockerfile 都能构建；main 的推送由 docker-publish.yml
      # 构建并发布，顺带覆盖这一步，这里跳过以免重复构建
      - name: Build production images
        if: github.event_name == 'pull_request'
        run: |
          docker build -t personal-memory-agent-api:test .
          docker build -f Dockerfile.web -t personal-memory-agent-web:test .

      - name: DOM regression tests
        run: node tools/dom-test.mjs

      - name: Static contract checks
        run: python tools/static-check.py

      - name: WCAG contrast checks
        run: python tools/contrast-check.py

      - name: Validate self-hosted database baseline as non-superuser app role
        env:
          PGPASSWORD: postgres
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq postgresql-client

          psql -h 127.0.0.1 -U postgres -d postgres -v ON_ERROR_STOP=1 <<'SQL'
          create role pma login password 'pma-ci-password' nosuperuser nocreatedb nocreaterole;
          SQL
          createdb -h 127.0.0.1 -U postgres -O pma pma

          test "$(psql -h 127.0.0.1 -U postgres -d postgres -Atc "select rolsuper from pg_roles where rolname='pma'")" = "f"

          PGPASSWORD=pma-ci-password psql             -h 127.0.0.1 -U pma -d pma             -v ON_ERROR_STOP=1             -f database/001_baseline.sql

          BAD_OWNERS=$(PGPASSWORD=pma-ci-password psql -h 127.0.0.1 -U pma -d pma -Atc "
            select c.relname || ':' || pg_get_userbyid(c.relowner)
            from pg_class c
            join pg_namespace n on n.oid = c.relnamespace
            where n.nspname = 'public'
              and c.relkind in ('r','p','S')
              and pg_get_userbyid(c.relowner) <> current_user
            order by c.relname
          ")
          if [ -n "$BAD_OWNERS" ]; then
            echo "Objects not owned by pma:"
            echo "$BAD_OWNERS"
            exit 1
          fi

      - name: Smoke test self-hosted API
        env:
          NODE_ENV: test
          PORT: 3000
          DATABASE_URL: postgres://pma:pma-ci-password@127.0.0.1:5432/pma
          OTP_PEPPER: ci-only-secret
          PREFERENCES_ENCRYPTION_KEY: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
        run: |
          npm start > /tmp/pma-api.log 2>&1 &
          API_PID=$!
          trap 'kill $API_PID || true' EXIT
          for i in $(seq 1 20); do
            if curl -fsS http://127.0.0.1:3000/api/health >/tmp/health.json; then
              cat /tmp/health.json
              exit 0
            fi
            sleep 1
          done
          cat /tmp/pma-api.log
          exit 1
